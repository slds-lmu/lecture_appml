Copy pasted from advml repo just in case. 

I believe `fairness` slides aren't used anywhere, and the `calibration` slides are used in `08_perf_calibration` (this) chunk. (tex codes are already included)